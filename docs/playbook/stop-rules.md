        ---
        title: Stop Rules Library
        change_type: minor
        band: A
        ---

        # Stop Rules Library

        > Stub. Replace with content and one worked example.

        **Format:** condition → action → owner → time box

1. Missing owner/date → Freeze scope → Chapter Lead → 24h
2. Approval SLA breach (48h) → Escalate to backup → Requestor → 24h
3. Metric regression beyond threshold → Rollback → Stream owner → 1 hour


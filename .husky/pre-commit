#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# Run guard on all docs
echo "🛡️ Running content guard..."
pnpm run guard || exit 1

# Check links
echo "🔗 Checking links..."
pnpm run links || exit 1

echo "✅ Pre-commit checks passed!"
